[package]
name = "headstate-proof-spike"
version = "0.1.0"
edition = "2021"

[lib]
name = "headstate_proof_spike"
path = "src/lib.rs"

[dependencies]
types = { git = "https://github.com/suchapalaver/lighthouse.git", branch = "stable" }
primitive-types = "0.12.2"
serde = { version = "1.0", features = ["derive"] }

[dev-dependencies]
insta = "1.39.0"
serde_json = "1.0"
